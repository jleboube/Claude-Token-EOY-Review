# AI Token Share Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# UNIFIED DEPLOYMENT CONFIGURATION
# ===========================================
# This single .env file is shared by all three instances:
# - Landing page (tokens.technojoe.dev)
# - Claude Token Share (claude-tokens.technojoe.dev)
# - OpenAI Token Share (openai-tokens.technojoe.dev)

# Node environment
NODE_ENV=production

# ===========================================
# X (TWITTER) OAUTH 2.0 CREDENTIALS
# ===========================================
# Get these from https://developer.x.com/en/portal/projects
#
# IMPORTANT: Configure THREE callback URLs in your X app:
# - https://tokens.technojoe.dev/api/auth/x/callback
# - https://claude-tokens.technojoe.dev/api/auth/x/callback
# - https://openai-tokens.technojoe.dev/api/auth/x/callback
#
X_CLIENT_ID=your_client_id_here
X_CLIENT_SECRET=your_client_secret_here

# ===========================================
# SESSION ENCRYPTION
# ===========================================
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_session_secret_here

# ===========================================
# NOTES
# ===========================================
# - Provider-specific settings (NEXT_PUBLIC_PROVIDER, NEXT_PUBLIC_APP_URL)
#   are set as build args in docker-compose.yml, not here
# - User API keys are entered at runtime and not stored
# - X OAuth tokens are stored in HTTP-only cookies for security
